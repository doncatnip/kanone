* docs for validators
* check how much core functionality could be ported to c/c++ without
  sacrifiyng an easy API - mainly to make composed Validators faster
  bottleneck atm is chaining alot with nested And/Or
* unit tests
